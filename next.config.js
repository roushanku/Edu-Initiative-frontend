/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;

module.exports = {
  images: {
    domains: ["images.pexels.com"], // Add the domain where the image is hosted
  },
};
